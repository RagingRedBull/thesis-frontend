import React from 'react'
import { Modal } from 'react-bootstrap'

const EditCompartment = ({show, setShow}) => {
    const handleClose = () => {
        setShow(false)
    }

    return (
        <Modal>

        </Modal>
    )
}

export default EditCompartment